{% extends 'base.html.twig' %}

{% block body %}
    <h1>{% trans %}Edit Tag{% endtrans %}</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{% trans %}edit{% endtrans %}" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('tag_index') }}">{% trans %}back to the list{% endtrans %}</a>
        </li>
    </ul>
{% endblock %}
