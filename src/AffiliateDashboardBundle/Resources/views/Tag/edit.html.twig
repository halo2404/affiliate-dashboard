{% extends 'base.html.twig' %}

{% block body %}
    <div class="page-header">
        <h1>{% trans %}Edit Tag{% endtrans %}</h1>
    </div>

    {{ form_start(edit_form, { 'style': 'horizontal' }) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{% trans %}edit{% endtrans %}" />
    {{ form_end(edit_form) }}

    <p>
        <a type="button" class="btn btn-default btn-xs" href="{{ path('tag_index') }}">{% trans %}back to the list{% endtrans %}</a>
    </p>
{% endblock %}
