{% extends 'base.html.twig' %}

{% block body %}
    <div class="page-header">
        <h1>{% trans %}Edit User{% endtrans %}</h1>
    </div>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{% trans %}edit{% endtrans %}" />
    {{ form_end(edit_form) }}

    <p>
        <a type="button" class="btn btn-default btn-xs" href="{{ path('user_index') }}">{% trans %}back to the list{% endtrans %}</a>
    </p>

    <ul>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="{% trans %}delete{% endtrans %}">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
